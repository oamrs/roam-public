[package]
name = "roam-python"
version = "0.1.0"
edition = "2021"
description = "Python bindings for the ROAM (OAM) framework"

[lib]
name = "roam"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.20", features = ["extension-module"] }
oam = { path = "../../" }
